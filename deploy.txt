# 前準備
az webapp stop --name hirahira-room --resource-group hirahira

# デプロイ
＃az group delete --name hirahira --yes　（全リソース削除のため実行しないこと）
az webapp delete --name hirahira-room --resource-group hirahira
az webapp up --name hirahira-room --runtime "PYTHON|3.11" --resource-group hirahira --location japanwest

# ポータル画面でVNETを追加する。

# マイグレーション
python manage.py migrate
python manage.py train_iris_model （初期モデルのトレーニング）

# 再起動
az webapp restart --name hirahira-room --resource-group hirahira
az webapp start --name hirahira-room --resource-group hirahira
az webapp show --name hirahira-room --resource-group hirahira --query state

# スーパーユーザー作成
python manage.py createsuperuser

# すべての情報を表示
az webapp show --name hirahira-room --resource-group hirahira

# ログを確認（問題がある場合）
az webapp log tail --name hirahira-room --resource-group hirahira

# 再起動
az webapp restart --name hirahira-room --resource-group hirahira


# 静的ファイルを収集
python manage.py collectstatic --noinput --settings=config.settings

# SECRET_KEY を生成
python -c "import secrets; import string; chars = string.ascii_letters + string.digits + '-_'; print(''.join(secrets.choice(chars) for _ in range(50)))"

# Gmailアプリパスワード
rdrl wtcj kidj gdcj





https://note.com/t_ak66/n/n791abe9fda8a

